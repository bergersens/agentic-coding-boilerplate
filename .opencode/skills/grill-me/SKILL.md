---
name: grill-me
description: Use ONLY when invoked via the /grill-me slash command. A user-invoked entry point that runs a grilling session on the topic the user provides. Do not trigger automatically.
---

# Grill Me

Run the `grilling` skill. Treat the user's message as the seed topic. If it references a file path or issue, read that file first before starting.
